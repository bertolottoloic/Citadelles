/*package fr.unice.polytech.startingpoint;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

public class RoleTest {

    Player player = new Player(1);

    @Test
    void MurdererTest(){
        Role murderer = new Murderer(player);
        assertEquals(1,murderer.getPosition());
        assertEquals(player,murderer.getPlayer());
        assertEquals(1,murderer.getNumberDistrict());
        assertEquals(2,murderer.getNumberGold());
    }

    @Test
    void ThiefTest(){
        Role thief = new Thief(player);
        assertEquals(2,thief.getPosition());
        assertEquals(player,thief.getPlayer());
        assertEquals(1,thief.getNumberDistrict());
        assertEquals(2,thief.getNumberGold());
    }

    @Test
    void WizardTest(){
        Role wizard = new Wizard(player);
        assertEquals(3,wizard.getPosition());
        assertEquals(player,wizard.getPlayer());
        assertEquals(1,wizard.getNumberDistrict());
        assertEquals(2,wizard.getNumberGold());
    }

    @Test
    void KingTest(){
        Role king = new King(player);
        assertEquals(4,king.getPosition());
        assertEquals(player,king.getPlayer());
        assertEquals(1,king.getNumberDistrict());
        assertEquals(2,king.getNumberGold());
    }

    @Test
    void BishopTest(){
        Role bishop = new Bishop(player);
        assertEquals(5,bishop.getPosition());
        assertEquals(player,bishop.getPlayer());
        assertEquals(1,bishop.getNumberDistrict());
        assertEquals(2,bishop.getNumberGold());
    }

    @Test
    void MerchantTest(){
        Role merchant = new Merchant(player);
        assertEquals(6,merchant.getPosition());
        assertEquals(player,merchant.getPlayer());
        assertEquals(1,merchant.getNumberDistrict());
        assertEquals(3,merchant.getNumberGold());
    }

    @Test
    void ArchitectTest(){
        Role architect = new Architect(player);
        assertEquals(7,architect.getPosition());
        assertEquals(player,architect.getPlayer());
        assertEquals(3,architect.getNumberDistrict());
        assertEquals(2,architect.getNumberGold());
    }

    @Test
    void WarlordTest(){
        Role warlord = new Warlord(player);
        assertEquals(8,warlord.getPosition());
        assertEquals(player,warlord.getPlayer());
        assertEquals(1,warlord.getNumberDistrict());
        assertEquals(2,warlord.getNumberGold());
    }
}*/
